# Not currently read at runtime (see src/config/env.ts) - kept here as a
# reference for the values baked into that file until real env-var
# injection (react-native-config or similar) is wired up.
API_BASE_URL=https://yectkl0tbg.execute-api.us-east-2.amazonaws.com/dev
