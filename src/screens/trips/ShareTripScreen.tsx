import React from 'react';
import { StubScreen } from '../../components/common/StubScreen';

export function ShareTripScreen() {
  return (
    <StubScreen
      title="Share trip"
      description="Granular sharing controls — choose exactly what buddies can see."
    />
  );
}
