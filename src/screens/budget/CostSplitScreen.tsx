import React from 'react';
import { StubScreen } from '../../components/common/StubScreen';

export function CostSplitScreen() {
  return (
    <StubScreen
      title="Cost split — settle up"
      description="Fair split of shared expenses between everyone on the trip."
    />
  );
}
