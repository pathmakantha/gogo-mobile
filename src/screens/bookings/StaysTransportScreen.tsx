import React from 'react';
import { StubScreen } from '../../components/common/StubScreen';

export function StaysTransportScreen() {
  return (
    <StubScreen
      title="Stays & transport"
      description="Book stays, trains and private drivers via tracked affiliate links to partner sites."
    />
  );
}
