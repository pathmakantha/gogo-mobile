import React from 'react';
import { StubScreen } from '../../components/common/StubScreen';

export function TourCustomizeScreen() {
  return (
    <StubScreen
      title="Customize tour"
      description="Swap or remove stops here — the trip price re-estimates live as you edit."
    />
  );
}
