import React from 'react';
import { StubScreen } from '../../components/common/StubScreen';

export function ProfileScreen() {
  return (
    <StubScreen
      title="Profile"
      description="Your account summary, trip history and links to Settings & Edit profile."
    />
  );
}
